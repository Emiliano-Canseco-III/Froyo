function froyo() {
  const froyoFlavor = [];
  const userOrder = prompt(
    "Hello! What froyo flayors would you like?",
    "Vanilla, Vanilla, vanilla, Strawberry, Coffee, coffee"
  );
  return;
}

console.log(froyo);
